{
  "name": "dotdev-calendar",
  "version": "0.2.1",
  "private": true,
  "description": "Userscript that adds a clash-aware calendar view to the DotDev 2026 sessions and my-list pages",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "jsdom": "^25.0.1",
    "playwright": "^1.61.1",
    "typescript": "^5.6.3"
  }
}
